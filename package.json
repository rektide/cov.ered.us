{
	"name": "cov.ered.us",
	"private": "true",
	"version": "1.0.0",
	"description": "Donation Acceptance demo app",
	"main": "index.js",
	"scripts": {
		"build:eb2b": "rm -rf build/browser_modules; mkdir -p build; cp -aur node_modules build/browser_modules; cd build/browser_modules; esm-bare-to-browser .",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"keywords": [
		"web payments",
		"web-payments",
		"webpayments",
		"payment request",
		"payment-request",
		"paymentrequest",
		"donation",
		"demo"
	],
	"author": "rektide de la faye <rektide@voodoowarez.com>",
	"license": "X11",
	"dependencies": {
		"braintree-web": "^3.31.0"
	},
	"devDependencies": {
		"esm-bare-to-browser": "^1.0.1"
	}
}
